public class MultilineIfConditionTest {
    public void method(Exception ex) {
        if (ex instanceof TypeA
                || ex instanceof TypeB
                || ex instanceof TypeC
                || ex instanceof TypeD) {
            handle(ex);
        }
    }
}
