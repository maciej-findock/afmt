public class Test {
    void m() {
        result =
            processPayment(
                this.validateRequestParameters(requestClass, processor),
                relationWrapper
            );

        a.b(c.d(e.f(
                arg1,
                arg2
        )));

        outer(inner(new Map<String, Object> {
            'k' => true
        }));
    }
}
