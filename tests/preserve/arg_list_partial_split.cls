public class A {
    void m() {
        Map<String, Object> result = SomeService.process((amount * 100).intValue(), reference, currencyCode, successUrl,
            getMethodName(), target, recurringMap != null, intentId, description, expiresAt, context);
    }
}
