public class A {
    protected override Map<String, SObject> getRecords(
            List<String> keys, fflib_ISObjectUnitOfWork uow, SomeLogger log) {
        Map<String, SObject> result = new Map<String, SObject>();
        return result;
    }

    public void singleParam(
            String value) {
        System.debug(value);
    }
}
