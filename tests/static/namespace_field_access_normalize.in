public class A {
    void m() {
        NS.ServiceClass.doWork();
        NS.ServiceClass.configure(param);
        ns.ServiceClass.doWork();
        String s = JSON.serialize(result);
        response.responseBody = Blob.valueOf(JSON.serialize(result));
    }
}
